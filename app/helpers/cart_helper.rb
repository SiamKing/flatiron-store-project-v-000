module CartHelper

    # def current_cart
    #   Cart.find_by(id: params[:id])
    # end

  # def checkout_helper
  #   @cart.status = "submitted"
  #   @cart.line_items.each do |item|
  #     current_item = Item.find(item.item_id)
  #     current_item.inventory -= item.quantity
  #     current_item.save
  #   end
  #   @user.current_cart = nil
  # end
end
