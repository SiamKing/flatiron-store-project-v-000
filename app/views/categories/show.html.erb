<h1><%= @category.title %></h1>

<ul>
<% @items.each do |item| %>
    <li><%= link_to item.title, item_path(item) %> - <%= number_to_currency(item.item_price) %>
        <%= button_to "Add To Cart", line_items_path(item_id: item) if user_signed_in? %></li>

<% end %>
</ul>
